
-----

first commit 
-----

feat: overhaul Reception workflow, add Credit Notes, and optimize startup performance

Procurement & Reception:
- Fix `ReceptionDialog` initialization order to prevent AttributeError on startup.
- Implement input locking mechanism: Product fields remain disabled until Invoice/BL validation.
- Fix validation logic to force-enable input fields immediately after header creation (no restart required).
- Ensure Purchase Order status updates to 'Completed' upon reception header validation.

Credit Notes (Avoirs):
- Implement full Credit Note management (Create, Edit, Delete).
- Add `CreditNoteManager` with "Delta" calculation logic to correctly adjust stock during edits (diff between old/new qty).
- Add `BatchSelectionDialog` to handle specific lot selection for returns.
- Implement "Strict Mode" for credit notes linked to specific reception vouchers.
- Enable stock restoration upon Credit Note deletion.

Performance & Architecture:
- Implement Lazy Loading in `MainWindow`: Tabs are initialized only when accessed to reduce startup time.
- Optimize `Database` class: Reduced connection pool size to 3, silenced verbose logging, and added a check to skip schema initialization if tables exist.
- Refactor `DashboardView` to fetch heavy KPI data in a background thread (`QThread`) to prevent UI freezing.

Statistics & Reporting:
- Fix unit display in Consumption Reports: Apply conversion factor to display 'Usage Units' (e.g., Tests) instead of fractional 'Stock Units' (e.g., 0.01 Box).
- Update Stock Movement Log to support 'Return_To_Supplier' transaction type.

UI/UX:
- Improve Logout confirmation dialog to default to "No" for safety.
- Refactor `base.py` logging configuration to reduce console noise.
-----

feat: add Reclamation module, fix Reception workflow and report units

Reclamation (Claims) Module:
- Implement `ReclamationTab` to list receptions with variance notes or product issues.
- Add `ReclamationDialog` for detailed management of claims.
- Allow editing of global reception notes (Header) via `update_variance_note`.
- Allow editing of specific product notes (Batch) via `update_batch_note`.
- Implement multi-select functionality to remove product notes in bulk.
- Disable direct table editing (`NoEditTriggers`) to prevent double-click conflicts.

Reception Workflow Fixes:
- Fix `ReceptionDialog` initialization order to prevent `AttributeError` on startup.
- Fix input locking logic: Product fields now correctly enable *immediately* after Invoice/BL validation without requiring a session restart.
- Ensure Purchase Order status updates to 'Completed' automatically upon header validation.

Statistics & Reporting:
- Fix unit display in Consumption Reports and KPIs:
  - Logic updated to multiply stock quantities by `Usage_Qty_Per_Stock_Unit`.
  - Reports now display Usage Units (e.g., "1 Test") instead of fractional Stock Units (e.g., "0.01 Box").

Database Managers:
- Update `ReceptionManager` with methods to fetch problematic receptions and update notes.
- Update `StatisticsManager` queries to support unit conversion.
-----

feat: release v1.6.0 - enhance alerts, fix KPIs and improve navigation

Summary of changes:

1. Alerts & Notifications (UI/Logic):
   - Implemented `ColorDelegate` in AlertsSection to force background coloring (Red/Orange/Green) based on expiry proximity, resolving QSS conflicts.
   - Fixed sorting logic to prioritize critical expirations (nearest dates first).
   - Handled 'N/A' values for `DaysRemaining` to prevent sorting errors.

2. Statistics & KPIs (Backend):
   - Refactored `get_kpi_summary` in StatisticsManager to correctly calculate Consumption and Waste using `Stock_Movement_Log`.
   - Changed KPI unit displays to Integers (removing unnecessary decimals).
   - Fixed SQL queries by removing references to non-existent columns (e.g., `Quantity_Consumed`) and using `Quantity` directly.

3. Inventory Navigation (Batches & Dispatch):
   - Implemented "View History" context menu action in both Batches and Dispatch tabs.
   - Added logic to navigate directly to the History Page (Page ID 6) and auto-fill the search with the product's Internal Barcode.
   - Added missing Context Menu policy to `DispatchTab`.
   - Fixed `NameError` by importing `BarcodeLineEdit` in `tabs_batches.py`.
   - Fixed `AttributeError` by correcting the refresh method call to `load_data`.

4. Versioning:
   - Bumped version to 1.6.0.
-----

feat(release): v1.6.1 - enhance login UX and add bulk barcode printing

Authentication (Keyboard Navigation):
- Smart Navigation: Pressing Enter on 'Username' now moves focus to 'Password'.
- Enter-to-Login: Pressing Enter triggers login only if fields are valid.
- Initial Focus: Cursor automatically targets the 'Username' field on startup.
- Validation: Prevent login attempts if fields are empty.

Reception & Barcodes:
- Implemented 'Print All' button in ReceptionDialog for mass generation.
- Added `BulkBarcodeSelectionDialog` to select/deselect items before printing.
- Added separator label logic to distinguish between product batches.
- Connected table double-click event to trigger item modification mode.
-----

feat(v1.7.0): Enhanced Dashboard Charts, FEFO Logic, and Inventory Pagination

MAJOR CHANGES:

1. Dashboard & Charts Overhaul:
   - Added dual-curve visualization: "Entrées" (Purchases) vs "Sorties" (Consumption).
   - Implemented date alignment and zero-padding logic to ensure continuous, non-jagged chart lines.
   - Added smart scatter labels for chart values with collision detection to prevent text overlap.
   - Improved tooltip and legend styling.

2. Inventory Performance Optimization:
   - Implemented Pagination in `BatchesTab` (50 rows per page) to handle large datasets efficiently.
   - Replaced heavy UI rendering with optimized table handling.
   - Added styling to the search/scan area ("Zone de Scan").

3. FEFO (First Expired, First Out) Implementation:
   - Enforced FEFO logic in Consumption and Dispatch tabs.
   - Added `FEFOSelectionDialog` to warn users when selecting a newer batch while an older one exists.
   - Smart sorting by Expiry Date then Reception Date.

4. Quick Actions & UX:
   - Added "Quick Consume" and "Quick Transfer" dialogs via context menu (Right-Click).
   - Added "View History by Barcode" shortcut for Admins.

5. Role-Based Access Control (RBAC):
   - Restricted "History" view to Admins only.
   - Hidden "Bon de Réception" option from Technicians in the context menu.
   - Hidden financial columns and totals for Technicians.

Fixed:
   - `setUniformRowHeights` attribute error in PySide6.
   - Chart animation crashes by handling series cleanup properly.